# Basic demo with Go and Docker


```
docker build -t docker-gs-ping:latest .
docker build -t docker-gs-ping:multistage -f Dockerfile.multistage .
```

For more;
[Resource](https://docs.docker.com/language/golang/build-images/)
